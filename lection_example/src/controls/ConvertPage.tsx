import React, { useState } from 'react' // в FC не обязателен
import PropTypes from 'prop-types'

const user = {
    rgb: '',
    hex: '',
    color: '',
  }

const ConvertPage = props => {
  return (
    <div>ConvertPage</div>
  )
}

ConvertPage.propTypes = {}

export default ConvertPage